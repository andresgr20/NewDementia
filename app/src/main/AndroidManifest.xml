<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andres.newdementia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
Ca
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CaregiverMain"></activity>
        <activity android:name=".ContactEmergency"></activity>
        <activity android:name=".PatientMain"></activity>
        <activity android:name=".PatientAct"></activity>
        <activity android:name=".CheckPatient"></activity>
        <activity android:name=".checkMap"></activity>
        <activity android:name=".AddPatient"></activity>
        <activity android:name=".AddGreetings"></activity>
        <uses-permission android:name="android.permission.WAKE_LOCK" />

    </application>

</manifest>